/** Mirrors `VehicleStatus` in `apps/api/src/modules/drivepass/vehicles/domain/value_objects/vehicle_status.py`. */
export enum VehicleStatus {
  Active = "ACTIVE",
  Inactive = "INACTIVE",
  Archived = "ARCHIVED",
}
