/** Mirrors `AuthProvider` in `apps/api/src/modules/identity/domain/value_objects/auth_provider.py`. */
export enum AuthProvider {
  Google = "google",
  Local = "local",
  Apple = "apple",
  Microsoft = "microsoft",
}
